{
  "name": "iitp-mtech-aidse-lecture-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build_static_site.mjs",
    "check": "node --check assets/app.js && node --check assets/analytics.js && node --check data/catalog.js && node --check data/i18n.js && node --check data/capstones.js && node --check data/open-resources.js && node tools/validate_content.mjs && node tools/validate_repo_discovery.mjs",
    "update-discovery": "node tools/validate_repo_discovery.mjs --update",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name iitp-mtech-aidse"
  }
}
